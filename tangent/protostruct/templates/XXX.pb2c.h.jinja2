#pragma once
// Generated by protostruct. DO NOT EDIT BY HAND!

#include "tangent/protostruct/pbwire.h"
#include "{{util.get_header_filepath(filedescr)}}"
#include "{{include_base}}.pb.h"

{% for descr in filedescr.enum_type %}
// Convert an enum value from a C++ binding object to a {{descr.name}} native
// structure
{{descr.name}} pb2c(const {{ctx.fqn_typename_cpp(descr)}}& proto);
{%- endfor %}

{% for descr in filedescr.message_type %}
// Copy data from a C++ binding object to a {{descr.name}} native structure
void pb2c(const {{ctx.fqn_typename_cpp(descr)}}& proto, {{descr.name}}* obj);
{% endfor %}

{% for descr in filedescr.enum_type %}
// Convert a native {{descr.name}} value to a protobuf C++ binding value
{{ctx.fqn_typename_cpp(descr)}} c2pb({{descr.name}} value);
{%- endfor %}

{% for descr in filedescr.message_type %}
// Copy a {{descr.name}} native structure to a protobuf C++ binding object
int c2pb(const {{descr.name}}& obj, {{ctx.fqn_typename_cpp(descr)}}* proto);
{% endfor %}


